#include "matrix_cipher.h"

int main() {
   //int choice;
   //char *start_str = malloc(100 * sizeof(char));

   printf("-- Matrix Encryption --\n");
   //printf("1 - Encrypt\n2 - Decrypt\n\n0 - Exit\n>");
   
   printf("%s\n", encode());
   
   //choice = get_int();

/*   switch(choice) {
      case 0:
         return 0;
      case 1:
         scanf("%[^\n]%*c", start_str);
         printf("%s\n", start_str);
         break;
      case 2:
         break;
      default:
         break;
   }*/

   return 0;
}